<%= form_with(model: sitio, local: true) do |form| %>
  <% if sitio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sitio.errors.count, "error") %> prohibited this sitio from being saved:</h2>

      <ul>
      <% sitio.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_field :descripcion %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="field">
    <%= form.label :ultimo_indexado %>
    <%= form.text_field :ultimo_indexado %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
